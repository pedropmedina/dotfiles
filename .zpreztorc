# Let's disable prompt for now as I'm using starship for now - https://starship.rs/
# zstyle ':prezto:module:prompt' theme 'pure'

zstyle ':prezto:module:autosuggestions' color 'yes'

zstyle ':prezto:load' pmodule \
         'directory' \
         'utility' \
         'completion' \
         'syntax-highlighting' \
         'history-substring-search' \
         'autosuggestions' \
